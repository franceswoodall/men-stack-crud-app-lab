<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css"> 
    <title><%= dog.name %></title>
</head>
<body>
    <h1>Edit <%= dog.name %></h1>
  <form action="/dogs/<%=dog.id%>?_method=PUT" method="POST">
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= dog.name %>" />

        <label for="breed">Breed:</label>
        <input type="text" name="breed" id="breed" value="<%= dog.breed %>" />

        <label for="doesTricks">Does Tricks?</label>
        <input type="checkbox" name="doesTricks" id="doesTricks" <% if (dog.doesTricks) { %>checked<% } %> />

        <button type="submit">Update Dog</button>
    </form>
    <a href="/dogs">Back to Dogs</a>
</body>
</html>